// Modifica proposta: integrazione apertura pagina "In Classe"
// Nota: l'entry reale importerà InClasseUI da js/in-classe.js e inizializzerà la UI sulla route specifica.
import { InClasseUI } from './js/in-classe.js';

export function openInClasse(params = {}) {
  // routing / load HTML / init UI - implementazione completa verrà committata
}